# WPC Website — 修改摘要

**日期：** 2026-09-08
**依據：** website_audit_2026-09-08.md

---

## 高優先修改（已完成）

### 1. 消除 index.html splash 跳轉
- 原本 `index.html` 是一個 20 秒自動跳轉至 `home.html` 的 splash 頁
- 現在 `index.html` 直接包含主頁完整內容
- splash 的蝴蝶背景照片保留為 hero section 的背景圖（深色半透明疊層）
- `home.html` 改為 301 redirect 至 `index.html`（向後相容）

### 2. 修正 colleberators.html 拼寫
- 複製為 `collaborators.html`（正確拼寫）
- 全站所有內部連結已更新
- 原 `colleberators.html` 改為 redirect 至 `collaborators.html`（向後相容）

### 3. blog.html 改造為社群嵌入頁面
- 移除「Under Construction」佔位內容
- 重新設計為完整的 blog 頁面，支援：
  - Instagram / Facebook / Threads 貼文嵌入
  - 圖片貼文（含 lightbox 預覽）
  - 文章型貼文（HTML 內容 + 外部連結）
  - 依標籤篩選
- 使用與主站一致的視覺風格（深藍 hero、卡片式佈局、導航列）
- 已加入全站導航列（所有頁面可見）
- 新增貼文只需編輯 `blog.html` 中的 `POSTS` 陣列

### 4. illustrations.html 隱藏
- 加入 `<meta name="robots" content="noindex, nofollow">`
- 從導航列移除

### 5. 圖片從 Google Drive 遷移準備
- 全站所有 `drive.google.com/thumbnail` 參照已更新為本地路徑 `imgs/web/ID.jpg`
- 包含 index.html（8 張）、projects.html（21 張）、collaborators.html JS 模板（37 張 marquee + avatar）
- splash 背景圖改為 `imgs/web/splash-bg.jpg`
- `.gitignore` 已更新允許 `imgs/web/` 目錄
- **⚠️ 需執行：** `python3 download_images.py` 下載並壓縮所有圖片至 `imgs/web/`

---

## 中優先修改（已完成）

### 6. 新增 sitemap.xml
- 列出 4 個主要頁面（index, projects, collaborators, blog）

### 7. 新增 robots.txt
- 允許所有爬蟲，指向 sitemap

### 8. 新增 meta tags
每頁 `<head>` 已加入：
- `<link rel="canonical">` — 標準化 URL
- Open Graph tags（og:title, og:description, og:image, og:url, og:type）
- Twitter Card tag（summary_large_image）
- Favicon 連結

### 9. 新增 Favicon
- `favicon.ico`（16/32/64px "WC" 深藍底白字）
- `apple-touch-icon.png`（180px）

### 10. 全站連結統一
- 所有頁面 logo 連結：`index.html`
- 所有頁面 anchor 連結：`index.html#talks`, `index.html#contact` 等
- 導航列一致：Home, Projects, Collaborations, Blog, Talks, Contact

---

## 待辦事項

- [ ] 執行 `python3 download_images.py` 下載圖片（需 `pip install Pillow requests`）
- [ ] 在 blog.html 的 `POSTS` 陣列中加入實際貼文內容
- [ ] git commit & push 所有變更

---

## 未修改（保留原樣）

- 視覺設計風格、配色（#14294b 深藍系）、排版、Tailwind CSS — 完全保留
- Talks & Media 區塊功能和 lightbox — 完全保留
- 所有外部連結（DOI, Google Scholar, LinkedIn, GitHub）— 未變動
- collaborators.html 的地圖、篩選器、卡片功能 — 完全保留
